# 기술 스택과 선택 이유

나열보다 **왜 그것을 골랐는지**를 남깁니다. 대안이 있었고 근거가 있었던 것만 적었습니다.

## 백엔드

| 기술 | 쓰임 | 고른 이유 |
|---|---|---|
| **Python** | 서비스 전반 | 로봇·ML 생태계(데이터셋 포맷, 시뮬레이터, 학습 스택)가 전부 파이썬. 경계를 넘나드는 비용이 가장 낮다 |
| **FastAPI** | 클라우드 API | Pydantic 모델이 그대로 스키마·검증·문서가 된다. 잡 명세와 매니페스트처럼 **계약이 곧 기능**인 도메인에 맞았다 |
| **PostgreSQL** | 상태 저장 · 잡 큐 | `SKIP LOCKED` 로 큐를 저장소 안에서 처리. 초기 규모에 브로커를 따로 두는 것보다 운영 표면이 작다 |
| **psycopg 3 (동기)** | DB 드라이버 | 라우트가 이미 스레드풀에서 돌고 스토리지 SDK도 동기라, DB만 async로 두면 같은 라우트에서 이벤트 루프를 블로킹한다. **드라이버를 하나로 통일**해 커넥션·타임아웃 규약이 두 벌이 되는 것도 막았다 |
| **Alembic** | 스키마 마이그레이션 | 앱 기동 시 직접 수행. 배포 파이프라인에 별도 마이그레이션 스텝을 두지 않아 배포물이 하나로 유지된다 |
| **PyArrow** | 데이터셋 검증 | 무결성 게이트가 메타데이터 파일을 직접 읽어 선언값과 실제 행수를 대조한다 |
| **공용 계약 라이브러리 (Pydantic)** | 게이트·잡 명세·승인 판정기 | 같은 규칙을 서버·엣지·검수가 각자 구현하면 반드시 갈린다. 판정기를 순수 함수 한 벌로 두고 모두가 가져다 쓴다 |
| **PyJWT (OIDC 검증)** | 인증 | 신원 공급자는 바꿀 수 있게 두고, 서버는 토큰 서명·클레임 검증만 한다. 인가(역할×범위)는 서버가 소유한다 |
| **LLM (제공자 선택형)** | 정의서·지시서 초안 | 초안 작성은 맡기되 **판정은 맡기지 않는다**. 출력은 계약 판정기를 통과해야 하고, 수치로 판정할 칸은 LLM이 채우지 못하게 막았다 |

## 엣지

| 기술 | 쓰임 | 고른 이유 |
|---|---|---|
| **httpx** | 에이전트 ↔ 클라우드 | 엣지 에이전트의 의존성을 의도적으로 최소화. 아웃바운드 HTTP 하나면 충분하다 |
| **SQLite** | 업로드 스풀 | 로봇 PC에 별도 프로세스를 띄우지 않고 크래시에도 살아남는 큐 |
| **하드웨어 인코더** | 영상 인코딩 | 로봇 PC GPU에 종속되는 부분. 인코딩까지 엣지에 두고 결과물만 업로드한다 |
| **gRPC / ZMQ** | 로봇 제어 · 명령 | 30fps 루프에서 REST 왕복은 예산에 안 맞는다 |

## 프런트

| 기술 | 쓰임 | 고른 이유 |
|---|---|---|
| **Next.js · React · TypeScript** | 운영 뷰, 랜딩 | 클라우드 API를 BFF로 프록시해 브라우저에 토큰을 노출하지 않는다. 브라우저는 httpOnly 세션 쿠키만 갖는다 |
| **openapi-typescript** | 운영 뷰 ↔ API 타입 | 서버 OpenAPI 스냅샷에서 타입을 생성하고, CI가 「생성 타입이 최신인가」를 검사한다. 손으로 쓴 타입이 서버와 어긋나는 일을 없앴다 |
| **Jinja2 서버 렌더** | 엣지 대시보드 | 폐쇄망에서 완결돼야 하는 화면. 빌드 산출물·CDN 의존이 없는 쪽이 안전하다 |

## 인프라

| 기술 | 쓰임 | 고른 이유 |
|---|---|---|
| **Docker · Compose** | 서비스 패키징, 로컬·엣지 배포 | 엣지가 오프라인이어도 이미지만 있으면 뜬다 |
| **오브젝트 스토리지 + presigned URL** | 데이터셋 저장 | 대용량이 API 서버를 거치지 않는다. 엣지에 자격증명을 주지 않는 것이 핵심 |
| **GitHub Actions** | CI · 이미지 릴리스 · 배포 | 서비스별 릴리스 워크플로를 분리하고 경로 필터로 바뀐 서비스만 돌린다 |
| **Argo (배치 잡)** | 학습 · 배치 평가 | 뜨고 죽는 GPU 워크로드를 상시 서버로 두지 않기 위해 |
| **Caddy (TLS 오버레이)** | LAN 공개 | 사내망에 열 때도 평문으로 두지 않는다. compose 오버레이 하나로 세 표면을 함께 덮는다 |
| **Makefile + 종단 시험** | 세우기 · 점검 · 복구 | 클론 하나로 스택을 세우고, 볼륨까지 지운 뒤 한 바퀴가 도는지 확인하는 시험을 진입점에 둔다 |

## 문서화

설계 결정을 코드와 같은 레포에서 관리하고 **국문이 원본, 영문을 동기화**하는 규칙을 세웠습니다.
결정을 내리는 문서와 그것을 한 장에 모으는 색인 문서를 분리하고, 색인에는 새 결정을 적지 않는다는
규칙을 명시했습니다. 색인이 원본과 어긋나는 순간 오히려 해롭기 때문입니다.

상태 표기도 사실만 적도록 고정했습니다 — **「구현」은 코드와 테스트가 있는 것, 「초안」은 문서만
있는 것**. 설계 문서가 구현된 것처럼 읽히는 문제를 막기 위한 장치입니다.
